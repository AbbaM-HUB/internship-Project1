# internship-project2
A sync system that keeps two databases in lockstep. DB 1 stores data in normal relational tables. DB 2 stores the same data as JSONB documents. When a record is created, updated, or deleted in DB 1, your system mirrors it to DB 2 automatically.
